import { Cylinders } from './jsx/allLiveEditors'

# Cylinders

`<Cylinders />` renders cylinder shapes and inherits from `<Command />`.

## Props

| Name       | Type         | Default | Description                         |
| ---------- | ------------ | ------- | ----------------------------------- |
| `children` | `Cylinder[]` | `[]`    | array of Cylinder markers to render |

### Cylinder

```js
type Cylinder = {
  pose: {
    position: { x: number, y: number, z: number },
    orientation: { x: number, y: number, z: number, q: number }
  }
  scale: {
    x: number,
    y: number,
    z: number
  },
  color: {
    r: number, // between 0 and 1
    g: number, // between 0 and 1
    b: number, // between 0 and 1
    a: number, // between 0 and 1
  },
}
```

<Cylinders />
